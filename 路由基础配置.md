# 0 feed
- ip地址 本机网络+子网掩码
- 路由 目的网络+子网掩码+下一跳
- OSPF,DHCP子网掩码
# 1 路由基础配置

## 1.校验位配置

```bash
crc 32 或 16
```
## 2.帧格式
- 配置POS 接口在物理层采用哪种帧格式进行传输
1. packet：IP数据包（网络层数据）
2. over：承载在...之上）
- 配置物理层传输标准采用北美的 SONET 标准，还是国际通用的 SDH 标准来“讲同一种语言”
```nginx
#1
Router-R3 (config) pos framing sonet
Router-R3 (config) pos flag s1s0 0
#2
Router-R3 (config) pos framing sdh
Router-R3 (config) pos flag s1s0 2
```
## 3.接口ip配置
- 1.loopback接口的ip地址  网络设备（如路由器、交换机）或计算机系统中的一个虚拟的、逻辑的接口。
```bash
#带宽和真实本机ip地址
Router-R3 (config)$bandwidth k为单位(1G=1000M=1000000k)
Router-R3 (config)$ip address 真实ip 子网掩码

#loopback接口的ip地址
Router-R3 (config)$loopback 0
Router-R3 (config)$ip address 回环接口ip地址 255.255.255.255

#静态路由
#目的网络 子网掩码 下一跳

#不知道目的地（目的子网不清晰，例如校园网边界路由器向外发），采用默认路由0.0.0.0和子网掩码0.0.0.0
Router-R3 (config)$ip route 0.0.0.0 0.0.0.0 下一跳
#向局域网内，如校园网内发送
Router-R3 (config)$ip route 聚合后的ip（局域网地址）子网掩码 下一跳

```


# 2 DHCP配置
DHCP地址池配置
- 缺省网关（Default Gateway）是本地网络中用于将发往外部网络或其它子网流量转发的路由器或路由接口。
- DHCP 服务器通常会为客户端分配缺省网关地址（例如 192.168.1.1），使客户端在目的地址不属于本地子网时，将数据包发送到该网关以便转发到互联网或其它网络。
- 缺省网关是“默认出口”，啥都不认识就找它；普通网关是“专用出口”，只负责某些特定的路。

```bash
# 1. 排除不分配的地址（如预留给服务器等）
ip dhcp excluded-address 低地址 高地址

# 2. 创建地址池并命名
ip dhcp pool 地址池名字

# 3. 指定可分配的网络范围和子网掩码
network 地址池ip地址 子网掩码

# 4. 设置分配给客户端的缺省网关
default-router 缺省网关ip地址

# 5. 设置租约时间（可选）
lease 天 时 分
# 例如：4小时30分钟 lease 0 4 30
```

# 3 OSPF路由协议配置
### feed
参与ip
- 配置本路由器的路由协议，ip填写本机参与转发的，也就是校园网的ip，area 0 就是 OSPF 的骨干区域
```bash
network (参与OSPF的网络)校园网ip 反码 area 0
area 0 range （参与的）校园网ip 正码 
```

```nginx
Router-R3 (config)router#ospf 进程号
```
# 4.访问控制列表配置
```bash 
#封禁udp端口
access-list 130 any any eq 1034
```
# 多路由连接
- 填写RG路由器路由表
- s0直连：s0输出端口和下一跳“端口”汇聚,往后同样，都是该端口
```bash
192.166.6.1/30
```
- 汇聚:二进制不同，“后面”全化0，不受.限制
- 子网掩码：被汇聚的变成主机位

### 规则
- 汇聚后不能和之前的相同
- 汇聚后可用地址数（2的32-n（网络位）
-2>ip数（端口数））
- 进1位法
### 划分子网
将一个ip地址划分为好几个ip
### 相关设备

#### 1.IPS
```bash
互联网
   │
防火墙（第一道防线） ← 阻挡明显的非法访问
   │
IPS（第二道防线） ← 深度检测允许的流量
   │
内部网络
   ├── Web服务器
   ├── 数据库服务器
   └── 办公网络
```
```bash
# 拓扑图1
[互联网] --- [路由器] --- [IPS设备] --- [服务器]
                    ↑              ↑
                端口1:有IP       端口2:无IP或管理IP
# 拓扑图2
[路由器] ---- [核心交换机] ---- [服务器群]
                    |
              [镜像端口] (SPAN)
                    |
              [入侵检测系统]
                 (IDS/IPS)
```
- 入侵检测，在没有交换机的情况下，入侵检测系统以透明模式串接在路由器与服务器之间，其业务端口不配置IP地址（做题:找同网段的路由器，的输出至终端的端口）
#### 2.TAP（分路器）
监听路由器之间的所有流量，链路中串接
# 交换机配置
## 1.vlan配置
- trunk：汇聚多个 VLAN（如办公、访客、语音）到一条链路，减少串联端口占用。
- access 是单VLAN，trunk 是“多VLAN跑同一根线”。
### cisco6500
顺序执行
```bash
#进入配置 
Enable
#配置域名
set vtp domain pku
set vtp mode server
#建立vlan
set vlan $ID号 name $vlan名字
set vlan $ID号 name $vlan名字
#分配vlan到端口(按照建立顺序)
Set vlan $ID $输入端口(连接客户机的)      
set vlan $ID $输入端口
#配置vlan trunk模式，封装vlan协议,端口为交换机连接路由器端口
set trunk $输出端口 on dot1q
#设置允许中继的vlan
set trunk $输出端口 vlan $ID1,$ID2
```
### Cisco3542
配置vlan
```bash
#witchport access vlan 100
exit
switch
```