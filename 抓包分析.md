# 0.feed
### 端口
|端口号|协议|
|---|---|
|443|https|
|21|ftp|
|80|http|
|53|DNS| 
服务器提供服务的端口，该行可能有welcome出现

### 区分
- 域名(domain)：www.pku.edu.cn
- url：协议+域名
- ip地址

### 协议号
protocol = 1（icmp）
6tcp
17 udp
# 1.sniffer

# 2.协议过程
## ip协议
- 我们常说的“TCP/IP协议”，实际上是指一整套协议族（包含了IP、TCP、UDP、HTTP、FTP等上百个协议）

|类|地址|
| :--- | :--- |
|A|1|
|B|128|
|C|192|

- 化二进制：除2 向上 左补0
- 化十进制: 128-1,8位，逢1相加
- 子网掩码: 10交界，前为网络地址，后为主机地址
- 受限广播：255.255.255.255
- 直接广播：网络位不变，主机位变1
- 第一个ip：网络地址加1，网络号加1
- 最后一个ip：直接广播减1，网络号加254
## DNS
- C 主向DNS
- R

- 断层后源地址为网关



## DHCP  
- UDP协议
```bash
Hardware address leangth= 6 bytes 
```
1. 释放,重新获得
- release
- renew
- 比4过程多一步
- 客户端ip  to 服务器ip  release
2. 客户端请求ip 4过程
- 客户端0.0.0.0始终255.255.255.255广播
- 服务器有自己的ip（sever ip ddress），也始终255.255.255.255广播
dis
offer
req
ack
3. 续约
客户端地址直接和服务器地址通信
req 
ack

4.  MAC
- Dst:Broadcast MAC地址
- 默认广播mac地址：  DLC:Destination 12个ffffffffffff
- py ad = client hw ad
5.单词
subnet musk 子网掩码
gateway address=default gateway网关
## TCP
seq=sequence
5. C-S seq=x
6. S-C seq=y,ack=x+1
7. C-S seq=x+1,ack=y+1
握手完成标号为7，（第三次握手）

- Port = 1101 填get /HTTP/1.1
- 协议号为6
# 3.命令分析
## ftp
```bash
ftp 域名
```
## ping
- Echo(ping)
- Echo reply
```bash
ping 域名
```
## tracert命令
发送不同TTL的ICMP报文，监听回应报文，探测计算机路径
-执行的命令
```bash
tracert 域名
```
- 特征
```bash
.      SOURCE| |Expert: Time-to-live 
         ICMP:ECHO
```
- 填写
```BASH
#
ICMP: protocol：ICMP
```